import 'package:flutter/material.dart';

class TaskTabWidget extends StatelessWidget {
  const TaskTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Task Tab Widget'));
  }
}
